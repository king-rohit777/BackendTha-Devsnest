/**
 * Just to test JS doc
 * Created for this reason , getting used at this application for this route
 * @param {string} a represents name 
 * @param {Boolean} b represents email existence 
 * @param {Number} c represents phone number 
 * @returns {object} an object with name , email and phone number.
 */

// function docTest(name, email, number) {
//     return {
//         name, email, number
//     }
// };
function docTest(a, b, c) {
    return {
        name=a, email=b, number=c
    }
};

doc